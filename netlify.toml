[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Optional but recommended: ensures all routes work correctly
[[redirects]]
  from = "/*"
  to = "/"
  status = 200